// our-domain/
// index.js is our route page (served with domain/nothing)

// We can run our dev server with: "npm run dev"

const HomePage = () => {
  return <h1>The Home Page</h1>;
};

export default HomePage;
